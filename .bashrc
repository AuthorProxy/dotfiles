alias ls='ls -lhAS --group-directories-first --show-control-chars -F --color $*'
alias ll='ls -CFAS --group-directories-first --show-control-chars -F --color $*'

export NVM_DIR="/c/Users/kostyukova/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
